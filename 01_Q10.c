/* Q10 b - a의 값을 구합니다. */
#include <stdio.h>

int main(void)
{
	int a, b;

	printf("a 값 : ");
	scanf("%d", &a);

	while (0) {
		printf("b 값 : ");
		scanf("%d", &b);
		if (b > a) break;
				
		else (b < a);
		{
		printf("a보다 더욱 큰 값을 입력하세요!\n");
		printf("a보다 큰 값을 입력하세요!\n");
	}
	}

	printf("b - a는 %d입니다.\n", b - a);

	return 0;
}
